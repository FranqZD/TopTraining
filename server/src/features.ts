/**
 * Interruptores de features a medias. El código (rutas, schema, notify) se
 * queda; si se retoma, se prenden. Mantener al día con `src/lib/features.ts`.
 *
 * Posts de texto en el grupo: apagados. Los comentarios de un entreno
 * (el post) siguen vivos.
 */
export const GROUP_TEXT_POSTS = false
