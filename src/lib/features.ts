/**
 * Interruptores de features a medias. El código (UI, API, schema) se queda;
 * si se retoma, se prenden. Mantener al día con `server/src/features.ts`.
 *
 * Posts de texto en el grupo: apagados. Los comentarios de un entreno
 * (el post) siguen vivos.
 */
export const GROUP_TEXT_POSTS = false
